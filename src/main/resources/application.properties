# Database Configuration
database.url=jdbc:postgresql://localhost:5432/mybaas
database.username=postgres
database.password=password

# Auth Configuration
auth.wellKnownUrl=https://example.com/.well-known/openid-configuration

# Redis Configuration (for multi-instance WebSocket events)
redis.enabled=false
redis.host=localhost
redis.port=6379
redis.password=